name: Job Name
on:
 push:
    branches:
        - '*'   
#env: 
 #BRANCH_NAME: ${{ github.job }}         
jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
        - name: Display First job Name
          run: echo " ${{ github.job}}"

    second-job:
        runs-on: ubuntu-latest
        steps:        
          - name: Display Second job Name
            run: echo "${{ github.job}}"


